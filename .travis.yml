language: node_js
node_js:
  - "lts/*"

cache:
  directories:
    - "node_modules"

branches:
  only:
  - nuxt-source

install:
  - npm install
  - npm run build
  - npm run generate

script:
  - echo "Skipping test"

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_ACCESS_TOKEN
  target-branch: master
  local-dir: dist
  on:
    branch: nuxt-source
